# Custom MCP Profiles

Store project-specific MCP providers and policy extensions here.

## Rules

- Keep custom providers capability-based, not vendor-bound.
- Register new providers in `../registry.yaml`.
- Align custom routing with `../policies.yaml` guardrails.
