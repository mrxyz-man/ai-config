# Quality Module

This module defines project quality gates and acceptance baselines.

Usage:
- Keep gates explicit and automatable where possible.
- Adjust thresholds intentionally and document reasons.
- Use this module as source of truth for delivery readiness checks.

Core files:
- `gates.yaml` for mandatory quality checks.
- `profile.yaml` for quality policy defaults.
