# Custom Agents

Add user-defined role files here.

## Rules

- Follow the same contract style as core roles.
- Register each custom role in `../registry.yaml`.
- Keep responsibilities explicit and non-overlapping with existing roles.
