# Interfaces Module

This module describes integration interfaces and shared data contracts used by the project.

Usage:
- Keep external interface definitions explicit and version-aware.
- Track compatibility expectations and ownership.
- Reference these files from project docs and workflows when needed.

Core files:
- `registry.yaml` for interface inventory.
- `schemas.yaml` for shared payload/schema declarations.
