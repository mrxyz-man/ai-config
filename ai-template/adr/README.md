# ADR Module

This module stores architecture decision records (ADRs) for important technical choices.

Usage rules:
- Record decisions that affect architecture, reliability, security, or team process.
- Keep one decision per file.
- Update status when a decision is replaced or deprecated.

Core paths:
- `records/` contains ADR documents.
- `template.md` is the baseline template for new ADR entries.
