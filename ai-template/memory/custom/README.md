# Custom Memory

Add project-specific memory artifacts in this directory.

## Rules

- Keep source references and confidence in custom entries.
- Avoid duplicating facts already present in project source-of-truth files.
- Do not store secrets or personal sensitive data.
