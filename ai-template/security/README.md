# Security Module

This module defines baseline security controls for AI-assisted project delivery.

Usage:
- Capture minimum required security checks and handling rules.
- Keep controls practical and aligned with project risk profile.
- Reference this module in review and release workflows.

Core files:
- `controls.yaml` for security control catalog.
- `secrets-policy.md` for secret handling and exposure prevention.
